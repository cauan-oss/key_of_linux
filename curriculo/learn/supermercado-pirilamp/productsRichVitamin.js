const stockProducts = require("./data.json");

function getProductsRichVitain () {
    let listWithVitamin = []
     for(let index = 0; index < stockProducts.length; index += 1){
          if(stockProducts[index].vitamins !== undefined) {

          }
     }
}
